# Program Analysis Lab

Sample project extension for defensive program-analysis workflows in an unnamed work-in-progress IDE.

```bash
npm install
npm run build
npm run validate
```

The compiled `dist/` entry point is committed so the host IDE never needs to install dependencies or run package scripts while installing the extension.
